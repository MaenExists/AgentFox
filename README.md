# AgentFox

AgentFox is a lightweight browser CLI for AI agents.

Current MVP architecture:

- `cli/`: `afox` command-line client
- `daemon/`: `afoxd` long-running browser daemon
- `protocol/`: shared request/response types for JSON IPC
